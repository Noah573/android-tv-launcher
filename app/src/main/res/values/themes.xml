<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.TVLauncher" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:colorBackground">@color/bg_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryDark">@color/bg_primary</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="Theme.TVLauncher.Overlay" parent="Theme.TVLauncher">
        <item name="android:windowBackground">@color/overlay_bg</item>
    </style>

    <style name="Theme.TVLauncher.Screensaver" parent="Theme.TVLauncher">
        <item name="android:keepScreenOn">true</item>
    </style>

</resources>
